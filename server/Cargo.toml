[package]
name = "server"
version = "0.1.0"
edition = "2021"
authors = ["David Amorim"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mio = { version = "0.8.10", features = ["os-poll", "net"] }
rand = "0.8.5"
